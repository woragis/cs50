#include <string.h>
#include <stdio.h>

void main(void)
{
  // string s = scanf("%d");
  // printf("The string: %s", s);
  int string_length = strlen(s)
  printf("Output: ");
  for (int i = 0; i < string_length; i++)
  {
    printf("%c", s[i]);
  }
  printf("%s\n");
}
